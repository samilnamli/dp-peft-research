model:
  name: distilbert
  pretrained_model: distilbert-base-uncased
  num_labels: 4

dataset:
  name: agnews
  train_split: train
  test_split: test

training:
  epochs: 20
  batch_size: 256
  physical_batch_size: 32
  learning_rate: 5e-4

peft:
  method: adapter
  adapter_reduction_factor: 16
